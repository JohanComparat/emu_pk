# Documentation build.  Deliberately no `classy` and no `optax`: ReadTheDocs
# cannot compile a Boltzmann solver, and `conf.py` mocks both for autodoc.
sphinx>=7
myst-parser>=2
furo
sphinx-copybutton
# The package itself is NOT listed here.  A `-e ..` would resolve against
# pip's working directory rather than this file's location, and ReadTheDocs
# runs pip from the repository root -- so it would point one level above the
# repo, at a directory with no pyproject.toml, and the build fails before it
# starts.  `.readthedocs.yaml` installs the package with `path: .` instead.
